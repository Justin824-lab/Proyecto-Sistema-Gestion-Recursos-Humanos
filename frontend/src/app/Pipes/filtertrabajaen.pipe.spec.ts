import { FiltertrabajaenPipe } from './filtertrabajaen.pipe';

describe('FiltertrabajaenPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltertrabajaenPipe();
    expect(pipe).toBeTruthy();
  });
});
