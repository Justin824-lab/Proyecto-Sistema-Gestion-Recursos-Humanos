import { FiltercontratoPipe } from './filtercontrato.pipe';

describe('FiltercontratoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercontratoPipe();
    expect(pipe).toBeTruthy();
  });
});
